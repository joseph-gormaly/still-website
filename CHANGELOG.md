# STILL Website Changelog

## [1.6.1] - 2026-02-09

### Added
- Open Graph meta tags to `index.html` and `projects.html`.
- Twitter Card meta tags to `index.html` and `projects.html` for social media previews.

### Enhanced
- ARIA roles for semantic clarity across `index.html` and `projects.html`.
  - Added `role="navigation"` to `<nav>` elements.
  - Added `role="banner"` and `role="region"` to `<section>` elements.
- Improved metadata description for better SEO visibility.
